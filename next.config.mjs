/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost', 'da.ckwapi.com', 'lms.creditcircle.in'],
  },
};

export default nextConfig;
